# Core dependencies
requests
python-dotenv
numpy
opencv-python-headless
flask
flask-cors

# YOLOv5 (install torch manually if needed)
torch
torchvision

ultralytics
pandas
tqdm
seaborn
requests
# DeepSORT with ReID
deep-sort-realtime
scipy

# Intel GPU (Iris Xe) support - optional
# Install via: pip install intel-extension-for-pytorch
# intel-extension-for-pytorch
